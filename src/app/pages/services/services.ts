import { Component, OnInit } from '@angular/core'; import { CommonModule } from '@angular/common'; import { FormBuilder, ReactiveFormsModule, Validators } from '@angular/forms'; import { ExtinguisherService } from '../../core/services/extinguisher'; import { Extinguisher } from '../../core/models/extinguisher.model'; import { ServiceRecord } from '../../core/models/service.model'; import { ServiceService } from '../../core/services/service'; import { NotificationService } from '../../core/services/notification'; import { ConfirmationService } from '../../core/services/confirmation';
@Component({ selector: 'app-services', standalone: true, imports: [CommonModule, ReactiveFormsModule], templateUrl: './services.html', styleUrl: '../extinguishers/operations.scss' }) export class Services implements OnInit { items: ServiceRecord[]=[]; extinguishers: Extinguisher[]=[]; showForm=false; editingId?:number; readonly form; constructor(fb:FormBuilder,private api:ServiceService,private extinguisherApi:ExtinguisherService,private notice:NotificationService,private confirm:ConfirmationService){this.form=fb.group({extinguisher_id:['',Validators.required],service_type:['Annual service',Validators.required],service_date:['',Validators.required],next_service_date:['',Validators.required],amount:[0,Validators.required],remarks:['']});} ngOnInit(){this.load();this.extinguisherApi.getAll().subscribe(r=>this.extinguishers=r.extinguishers);} load(){this.api.getAll().subscribe({next:r=>this.items=r.services,error:()=>this.notice.error('Unable to load service records.')});} reset(){this.form.reset({service_type:'Annual service',amount:0});this.editingId=undefined;this.showForm=false;} save(){if(this.form.invalid)return;const data={...this.form.getRawValue(),extinguisher_id:Number(this.form.value.extinguisher_id),amount:Number(this.form.value.amount)} as ServiceRecord;(this.editingId?this.api.update(this.editingId,data):this.api.create(data)).subscribe({next:()=>{this.notice.success('Service record saved successfully.');this.reset();this.load();},error:()=>this.notice.error('Unable to save service record.')});} edit(item:ServiceRecord){this.editingId=item.id;this.form.patchValue({...item,extinguisher_id:String(item.extinguisher_id)});this.showForm=true;} async remove(item:ServiceRecord){if(!item.id||!await this.confirm.open({title:'Delete service record?',message:'This record will be permanently removed.',confirmLabel:'Delete record'}))return;this.api.remove(item.id).subscribe({next:()=>{this.notice.success('Service record deleted.');this.load();},error:()=>this.notice.error('Unable to delete service record.')});}}
