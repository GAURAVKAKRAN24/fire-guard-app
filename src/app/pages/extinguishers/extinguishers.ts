import { Component, OnInit } from '@angular/core'; import { CommonModule } from '@angular/common'; import { FormBuilder, ReactiveFormsModule, Validators } from '@angular/forms'; import { CustomerService } from '../../core/services/customer'; import { Customer } from '../../core/models/customer.model'; import { Extinguisher, ExtinguisherResponse } from '../../core/models/extinguisher.model'; import { ExtinguisherService } from '../../core/services/extinguisher'; import { NotificationService } from '../../core/services/notification';
@Component({ selector: 'app-extinguishers', standalone: true, imports: [CommonModule, ReactiveFormsModule], templateUrl: './extinguishers.html', styleUrl: './operations.scss' })
export class Extinguishers implements OnInit { items: Extinguisher[] = []; customers: Customer[] = []; showForm = false; editingId?: number; readonly form; constructor(fb: FormBuilder, private api: ExtinguisherService, private customerApi: CustomerService, private notice: NotificationService) { this.form = fb.group({ customer_id: ['', Validators.required], extinguisher_no: ['', Validators.required], type: ['ABC', Validators.required], capacity: ['', Validators.required], purchase_date: [''], last_service_date: [''], next_service_date: [''], status: ['Active', Validators.required] }); } ngOnInit() { this.load(); this.customerApi.getCustomers(1, 100).subscribe(r => this.customers = r.customers); } load() { this.api.getAll().subscribe({ next: r => this.items = r.extinguishers, error: () => this.notice.error('Unable to load extinguishers.') }); } edit(item: Extinguisher) { this.editingId = item.id; this.form.patchValue({ ...item, customer_id: String(item.customer_id) }); this.showForm = true; } reset() { this.form.reset({ type: 'ABC', status: 'Active' }); this.editingId = undefined; this.showForm = false; } save() { if (this.form.invalid) return; const data = { ...this.form.getRawValue(), customer_id: Number(this.form.value.customer_id) } as Extinguisher; const request = this.editingId ? this.api.update(this.editingId, data) : this.api.create(data); request.subscribe({ next: () => { this.notice.success(`Extinguisher ${this.editingId ? 'updated' : 'registered'} successfully.`); this.reset(); this.load(); }, error: () => this.notice.error('Unable to save extinguisher.') }); } }
