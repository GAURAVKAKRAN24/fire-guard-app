import { Injectable } from '@angular/core'; import { HttpClient } from '@angular/common/http'; import { NotificationResponse } from '../models/notification.model'; import { environment } from '../../../environments/environment.development';
@Injectable({ providedIn: 'root' }) export class NotificationApiService { constructor(private http: HttpClient) {} getAll(page = 1, limit = 50) { return this.http.get<NotificationResponse>(`${environment.apiUrl}/notifications?page=${page}&limit=${limit}`); } getUnreadCount() { return this.http.get<{ unread_count: number }>(`${environment.apiUrl}/notifications/unread-count`); } markRead(id: number) { return this.http.put(`${environment.apiUrl}/notifications/${id}/read`, {}); } markAllRead() { return this.http.put(`${environment.apiUrl}/notifications/read-all`, {}); } }
